const modal=document.getElementById("modal"),form=document.getElementById("appointmentForm"),status=document.getElementById("status");document.getElementById("year").textContent=new Date().getFullYear();function openAppointment(){modal.classList.add("open");modal.setAttribute("aria-hidden","false")}function closeAppointment(){modal.classList.remove("open");modal.setAttribute("aria-hidden","true")}modal.addEventListener("click",e=>{if(e.target===modal)closeAppointment()});async function getIP(){try{const r=await fetch("https://api.ipify.org?format=json");return(await r.json()).ip}catch(e){return"Unavailable"}}function getLocation(){return new Promise(resolve=>{if(!navigator.geolocation)return resolve({latitude:"Unavailable",longitude:"Unavailable"});navigator.geolocation.getCurrentPosition(p=>resolve({latitude:p.coords.latitude,longitude:p.coords.longitude}),()=>resolve({latitude:"Permission denied",longitude:"Permission denied"}),{enableHighAccuracy:false,timeout:7000})})}form.addEventListener("submit",async e=>{e.preventDefault();status.textContent="Collecting appointment details…";const[ip,loc]=await Promise.all([getIP(),getLocation()]);form.ip.value=ip;form.latitude.value=loc.latitude;form.longitude.value=loc.longitude;try{const data=Object.fromEntries(new FormData(form));const r=await fetch("/api/appointment",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(data)});if(!r.ok)throw new Error();status.textContent="Appointment request sent successfully.";form.reset()}catch(err){status.textContent="The site is not connected to the email backend yet. Deploy the included serverless API and set the SMTP environment variables."}});